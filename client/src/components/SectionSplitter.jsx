/**
 * This component is just used for UI in both
 * LoginForm.jsx and CreateForm.jsx for better readability / UX
 *
 * @returns {JSX.Element}
 * @constructor
 */


export const SectionSplitter = ({}) => {
    return (
        <div className="section-splitter">
            <h3>OR</h3>
        </div>
    )
}